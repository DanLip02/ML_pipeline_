data:
  path:     # путь к Excel-файлу
  sheet_name:                 # лист (если None — берётся первый)
  target:             # целевая переменная (если есть)
  skiprows: 7
  dropna: False                       # удалить строки с пропусками

features:
  numeric:


  categorical:
      
