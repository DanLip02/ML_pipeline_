data:
  path:       # path to xlsx file
  sheet_name: "smth"                 # sheet_name
  target: "smth"             # target
  skiprows: 7
  dropna: False                       # drop rows with NaN
  skip: "skip"
  date_column: "report_date"
  filter_columns:
    - "report_date"
    - "global_id"
    -
features:
  numeric:

  categorical:
